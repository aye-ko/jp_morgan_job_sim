# jp_morgan_job_sim
A project to complete the job simulation posted by JP Morgan on Forage. 
Comprises a Prediction Model for Natural Gas that, given a set of data, uses both linear and sinusoidal regression, with linear regression being more accurate. I suspect this is because each month gets its own coefficient due to one-hot encoding. 
